# DylanDevelops Homebrew Tap

A [Homebrew](https://brew.sh/) tap for my tools.

## Usage

Install a formula directly:

```bash
brew install DylanDevelops/tap/<formula>
```

This automatically adds the tap, so there's no separate `brew tap` step.

> [!NOTE]
> Homebrew 6 and later require non-official taps to be trusted before they can
> be installed (this is enabled by default). If you see a trust error, run the
> following once, then re-run the install command:
>
> ```bash
> brew trust --tap DylanDevelops/tap
> ```

## Available Formulae

| Formula                                         | Description                             |
| ----------------------------------------------- | --------------------------------------- |
| [`tmpo`](https://github.com/DylanDevelops/tmpo) | Minimal CLI time tracker for developers |

## License

Formulae in this tap are generated by [GoReleaser](https://goreleaser.com/) and
are licensed under the same terms as their respective projects.
